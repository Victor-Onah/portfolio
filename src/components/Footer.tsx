const Footer = () => {
	return (
		<footer className="px-4 py-12 bg-slate-200">
			<h4 className="font-semibold text-center">
				Onah Victor&copy; {new Date(Date.now()).getFullYear()}
			</h4>
		</footer>
	);
};

export default Footer;
